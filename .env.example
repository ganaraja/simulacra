# Copy this file to .env and set values for your environment.
# Do not commit .env (it is in .gitignore).
#
# Backend loads .env from the repo root when you run uvicorn from the repo root.
# For frontend (Vite), copy .env to src/frontend/.env so VITE_* variables are used,
# or set them in the shell before running npm run dev.

# -----------------------------------------------------------------------------
# Backend (FastAPI + ADK)
# -----------------------------------------------------------------------------

# Required for running the debate (Google ADK / Gemini).
# Get a key: https://aistudio.google.com/app/apikey
# Google API Configuration
GOOGLE_API_KEY=your_google_api_key_here
GOOGLE_API_MODEL=gemini-2.0-flash-exp

# Optional. Host and port for the backend server (used when running uvicorn).
# Defaults: BACKEND_HOST=127.0.0.1, BACKEND_PORT=8000
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# Optional. Comma-separated origins for CORS (e.g. frontend URL).
# Default: http://localhost:3000,http://127.0.0.1:3000
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# -----------------------------------------------------------------------------
# Frontend (Vite/React)
# -----------------------------------------------------------------------------
# Optional. API base URL for fetch(). Empty = same origin (use Vite proxy in dev).
# Set to full backend URL when frontend is served from a different origin.
VITE_API_BASE=
